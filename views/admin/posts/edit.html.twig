{% extends 'admin/base_admin.html.twig' %}

{% block title 'Edition' %}

{% block body %}
    <h1 class="mb-5">Editer l'article {{ post.name }}</h1>

    <form action="{{ path('admin.posts.edit', {'id': post.id}) }}" method="post">
        {% include 'admin/posts/form.html.twig' %}
        <button class="btn btn-primary" type="submit">Editer</button>
    </form>
{% endblock %}
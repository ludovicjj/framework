{% extends 'base.html.twig' %}

{% block title post.name %}

{% block body %}
    <h1>{{ post.name }}</h1>
    <p class="text-muted">{{ post.created_at | ago }}</p>
    <p>
        {{ post.content | nl2br }}
    </p>
{% endblock %}